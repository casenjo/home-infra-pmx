container_templates:
  hosts:
    golden_image:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/ansible

container_docker_hosts:
  hosts:
    pihole_test:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/ansible
